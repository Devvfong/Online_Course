<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %> - Online Course Platform</title>
    <link href="/css/style.css" rel="stylesheet">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body class="bg-gray-50 min-h-screen flex items-center justify-center">
    <div class="max-w-md w-full space-y-8">
        <div>
            <div class="flex justify-center">
                <a href="/" class="flex items-center">
                    <i class="fas fa-graduation-cap text-4xl text-primary mr-2"></i>
                    <span class="text-2xl font-bold text-gray-800">CourseHub</span>
                </a>
            </div>
            <h2 class="mt-6 text-center text-3xl font-extrabold text-gray-900">
                Sign in to your account
            </h2>
            <p class="mt-2 text-center text-sm text-gray-600">
                Or
                <a href="/auth/signup" class="font-medium text-primary hover:text-secondary">
                    create a new account
                </a>
            </p>
        </div>
        
        <form class="mt-8 space-y-6" action="/auth/login" method="POST">
            <div class="card">
                <% if (error) { %>
                    <div class="mb-4 bg-red-50 border border-red-200 text-red-700 px-4 py-3 rounded">
                        <%= error %>
                    </div>
                <% } %>
                
                <div class="space-y-4">
                    <div>
                        <label for="email" class="block text-sm font-medium text-gray-700">Email address</label>
                        <input id="email" name="email" type="email" required class="form-input mt-1" placeholder="Enter your email">
                    </div>
                    
                    <div>
                        <label for="password" class="block text-sm font-medium text-gray-700">Password</label>
                        <input id="password" name="password" type="password" required class="form-input mt-1" placeholder="Enter your password">
                    </div>
                </div>
                
                <div class="mt-6">
                    <button type="submit" class="btn btn-primary w-full">
                        <i class="fas fa-sign-in-alt mr-2"></i>
                        Sign in
                    </button>
                </div>
                
                <div class="mt-4 text-center">
                    <a href="/" class="text-sm text-gray-600 hover:text-primary">
                        ← Back to home
                    </a>
                </div>
            </div>
        </form>
        
        <!-- Demo Accounts -->
        <div class="card">
            <h3 class="text-lg font-medium text-gray-900 mb-4">Demo Accounts</h3>
            <div class="space-y-2 text-sm">
                <div class="p-2 bg-gray-50 rounded">
                    <strong>Owner:</strong> owner@example.com / owner123
                </div>
                <div class="p-2 bg-gray-50 rounded">
                    <strong>Admin:</strong> admin@example.com / admin123
                </div>
                <div class="p-2 bg-gray-50 rounded">
                    <strong>Student:</strong> student@example.com / student123
                </div>
            </div>
        </div>
    </div>

    <script src="/js/main.js"></script>
</body>
</html>